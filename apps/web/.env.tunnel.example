# ---------------------------------------------------------------------------
# Test temporaire via tunnel Cloudflare (voir TUNNEL-TESTING.md a la racine)
# Ce fichier n'est PAS charge automatiquement par Vite (nom non standard).
# Pour l'utiliser : copier son contenu dans .env.local (deja ignore par git
# via *.local dans apps/web/.gitignore) et remplacer l'URL par celle donnee
# par `cloudflared tunnel --url http://localhost:3000` sur le back.
# ---------------------------------------------------------------------------
VITE_API_URL=https://<url-generee-par-cloudflared-pour-le-back>.trycloudflare.com
